let valores = [8,1,7,4,2,9]
valores.sort()
valores.indexOf(1)
/*for (let pos=0;pos<valores.length;pos+=1){
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`) // Varrer a Lista.
}

// Esse modelo abaixo é um maneira mais fácil ainda de varrer  a lista. utilizando o *for in

for(let pos in valores){
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
}
valores.indexOf(1)
*/